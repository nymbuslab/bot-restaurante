node_modules/
*.log

# Dados de runtime (banco, sessões WhatsApp, tenants)
data/*.db
data/tenants/

# Backups manuais (contêm dados de cliente — nunca versionar)
backups/

# Ambiente / temporários
.env
.mcp.json
*.tmp
