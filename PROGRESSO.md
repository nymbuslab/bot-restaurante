# Progresso do Projeto

> Detalhes de arquitetura e stack ficam em `CLAUDE.md`. Aqui entram apenas etapas: fazendo, próximos passos e concluído.

## 🔄 Em Andamento

_(nada no momento)_

## 📋 Próximos Passos

### Em aberto

- [ ] **(P1) Publicar o `.exe` do agente de impressão** — `npm run dist` (gera o instalador NSIS) → servir `/downloads/` no Express e hospedar o `.exe` → atualizar o link de download em `admin.html` (remover "em breve"/`aria-disabled`). Antes disso, validar manualmente no Windows (login → configurar impressora Rede/Serial → teste → pedido real imprimindo sozinho). Auto-update segue **desligado** até o instalador ser assinado (code signing) — religar só com `verifyUpdateCodeSignature`. App já construído/mergeado (ver ✅ Concluído).

#### Adequação LGPD — pendências futuras (Fases 1–4 concluídas; ver ✅ Concluído)

> Reanálise `/lgpd-checker`: ⚠️ PARCIALMENTE CONFORME (90), **zero itens ALTO/CRÍTICO**. Nenhuma destas bloqueia.

- [ ] **R-01 (operacional)** Confirmar/arquivar os DPAs dos subprocessadores (Supabase, Stripe, Resend, Geoapify) e anotar em `docs/lgpd/subprocessadores.md`.
- [ ] **R-02 (operacional)** Confirmar a região de Resend e Geoapify e registrar no doc.
- [ ] **O-01 (jurídico)** Revisão dos textos (Termos/Privacidade) por advogado — casa com a pendência standing de revisão legal.
- [ ] **(decisão) Região do Supabase = EUA** — migrar pra Brasil (`sa-east-1`) é opcional; hoje está legal com a divulgação na Política.

### Pendências operacionais (standing)

- **(produto) Pedido de teste #3** no `sabor-d-casa` pode ser apagado pelo painel (resíduo do cardápio web; `PUBLIC_URL`/`CARDAPIO_LINK_SECRET` já setados e no ar na v28).
- **(operacional) GitHub cache:** SHAs antigas com PII podem persistir em cache/forks — purga total exige ticket ao Support.
- **(produto) Falar com Suporte:** o WhatsApp de suporte fica vazio até ser preenchido em Configurações Master (ou env `SUPORTE_WHATSAPP`); enquanto vazio, o card "Precisa de ajuda?" fica oculto no painel do cliente.

> **Limpeza `session-*` no Fly (P2): RESOLVIDA/moot** — o volume `bot_dados` foi **destruído** no redeploy de 2026-06-15 (app stateless, sem volume); o fs do container é efêmero e nasce limpo a cada boot. Não há resíduo do whatsapp-web.js para limpar. Item removido dos Próximos Passos.

## ✅ Concluído

- [x] **Cardápio web — cards de produto horizontais (imagem à esquerda)** — redesign dos cards de produto de verticais (imagem no topo) para **horizontais** (imagem à esquerda 120–130px, nome/preço à direita), grid de 1 coluna no mobile e 2 colunas no desktop (≥700px), título de categoria com filete de destaque (`public/cardapio.css`). **Fix de quebra:** em telas estreitas (~320px e faixa 700–1000px em 2 colunas) o preço longo "a partir de R$ X" empurrava o botão "+ Adicionar" pra fora da borda — `.cd-card-rodape` ganhou `flex-wrap` (botão desce inteiro) + preço com reticências; removidos os overrides `flex-shrink:0`/`text-overflow:clip`. Validado no Playwright (320/390/700/1280, desktop+mobile). — 2026-06-28
- [x] **Dashboard + reorg do painel** — nova aba Dashboard como landing (métricas com tendência vs ontem, últimos pedidos, status WhatsApp, ações rápidas); sidebar renomeada (Cardápio → Produtos, Conexão removida); Conexão movida para sub-aba de Configurações; novas sub-abas Horários e Pagamentos; editor de item em 4 abas (Principal/Composições/Opcionais/Variações) com campo precoCusto; Google Stitch MCP configurado. — 2026-06-28
- [x] **R-03 (LGPD) — retenção da trilha de auditoria** — `auditoria.limparAntigos(meses=24)` apaga registros de `auditoria` com +24 meses (DELETE por `criado_em`, índice `auditoria_criado_em_idx`; trilha sem PII, valor temporal); agendado no `index.js` (75s após boot + a cada 24h), best-effort no padrão dos jobs de retenção de pedidos/clientes. **Único item de código restante da adequação LGPD.** 174/174 testes + check. Commit `c8d4c6a`. — 2026-06-27
- [x] **Stripe go-live — teste E2E com cartão real + estorno** — teste de ponta a ponta validado: cadastro → checkout → `trialing`/`active` com cartão real → conferência dos eventos no webhook → estorno. Stripe em produção (live), cobrando de verdade, sem pendências. — 2026-06-27
- [x] **Revisão jurídica dos textos legais (Termos/Privacidade)** — Termos e Privacidade revisados por advogado, refletindo o fluxo do cardápio web, limite de responsabilidade, prazo de retenção e figura do DPO. Documentos legalmente conformes. — 2026-06-27
- [x] **Variações no item do cardápio (opções com preço + estoque)** — feature estilo iFood "a partir de R$ X": item sem preço fixo lista variações (ex.: sabores de refrigerante); o cliente/PDV escolhe várias com quantidade, o preço soma, e **cada variação dá baixa no próprio estoque** (chave `item.id::variacao.id`, atômica via `baixarEstoqueTx`) — o que opcionais/composição não faziam. Nova seção "Variações" no editor (nome/preço/estoque/mín; preço base 0 permitido). Módulo puro `public/variacoes.js` (normalizar/`precoAPartir`/`avaliarVariacoes` regra ≥1/`todasEsgotadas`); projeção+recálculo em `cardapio-web.js`/`pdv.js` (sem vazar contagem); front cliente + PDV + comanda. **174/174 testes + check.** Validado no navegador ponta a ponta: ids únicos, soma, bloqueio sem escolha, baixa por variação (Laranja 4→2). **Ressalva:** só vale pros clientes após `fly deploy`. — 2026-06-27
- [x] **Cardápio — barra de categorias rolável no celular** — ao tocar numa categoria, a barra rola sozinha (direita/esquerda) até centralizar o chip ativo, em vez de zerar o scroll e esconder a categoria escolhida (`renderCategorias` reconstruía a barra e perdia a posição). + inércia de arraste no iOS (`-webkit-overflow-scrolling: touch`) em `public/cardapio.{js,css}`. Validado no navegador (Playwright, viewport 390×844, `sabor-d-casa` com 10 categorias: rola pra direita ao clicar na ponta, volta pra esquerda no início). No ar. — 2026-06-27
- [x] **Padronização de nomes do cardápio (Title Case PT-BR)** — nome de produto, categoria e opcional ficam num padrão consistente **ao digitar** (formata ao sair do campo no editor) **e ao salvar** (o servidor re-padroniza em `PUT /api/cardapio` antes de gravar → durável, imune a cache/painel desatualizado, que chegou a sobrescrever a 1ª tentativa). Regras: 1ª palavra maiúscula, conectivos minúsculos (`de`/`com`/`do`…), hífen preservado (`x-tudo`→`X-Tudo`), espaços colapsados; **preserva** medidas (`1,5L`/`350ml`) e abreviações (`c/`/`s/`). Módulo puro `public/texto.js` (`tituloPt` + `padronizarNomesCardapio`, 11 testes), usado no front (`app.js`, blur) e no back (`servidor.js`). Backlog do `sabor-d-casa` (**28 nomes**) corrigido e **confirmado no ar**. **155/155 testes** + validado no navegador (Playwright). — 2026-06-27
- [x] **Agente de impressão desktop — Plano B (app Electron `agente-impressora/`)** — construído o app que imprime os pedidos do cardápio web **automaticamente** numa térmica, sem o painel aberto: login (cofre do SO via `safeStorage` para o refresh token), polling de `/api/agente/pendentes` com backoff exponencial, transportes **Rede (TCP 9100)** e **Serial (COM)** — **USB desabilitado** com erro honesto (não marca impresso sem imprimir) —, impressão ESC/POS crua reusando `public/comanda.js`+`serial-escpos.js`, UI espelhando o painel, ícone na bandeja (tray) e empacotamento `electron-builder` (NSIS). Auto-update **desligado** até o instalador ser assinado (evita RCE/supply-chain). Robustez: não reimprime cupom quando só a confirmação no servidor falha, recarrega config do tenant a cada 5 min, caches de config/login, envia todas as vias numa conexão só. Construído por subagent-driven-development (8 tasks) + 2 rodadas de `/code-review` (10 achados corrigidos). **144/144 testes** + `node --check`. Mergeado na `main`. **Ressalvas:** falta **validação manual no Windows** (sem ferramenta de hardware aqui) e **publicar o `.exe`** (item à parte em Próximos Passos). — 2026-06-27
- [x] **Backend do agente de impressão desktop (Plano A)** — preparado o backend do futuro app desktop de impressão automática: coluna `pedidos.impresso_em` (migration aplicada em produção), `pedidos.pendentes()` (pedidos do cardápio web ainda não impressos — `impresso_em IS NULL` + `recebido_em IS NULL`, `LIMIT 50`) e `pedidos.marcarImpresso()` (idempotente — sem reimpressão/duplicata); 4 rotas `/api/agente/*` (login + refresh com o refresh token **no corpo** p/ o cofre do SO, `GET pendentes`, `POST .../impresso`) reusando `exigeAuth` + rate limit; seção "Impressora — app desktop" no painel (Configurações → Impressora) com passo a passo e botão "Baixar (em breve)" (desabilitado até o app sair). Spec e plano em `docs/superpowers/`. **129/129 testes + `npm run check`.** Backend aditivo/seguro, **sem consumidor** até o app Electron (Plano B). Mergeado na `main`. — 2026-06-26
- [x] **Composição selecionável (subgrupos com regras no item)** — a composição do item deixou de ser texto só-exibição e virou **subgrupos selecionáveis** pelo cliente, com **obrigatório / mín / máx** por subgrupo (cadastro no editor). No **cardápio web** e no **PDV** o cliente monta o prato escolhendo em cada grupo (máx=1 → única/radio; máx>1 → múltipla/checkbox com trava no máx; obrigatório bloqueia "Adicionar" até o mín); composição é **grátis** (não soma preço) — **opcionais** seguem como estão (texto pago). **Servidor é fonte de verdade** (valida/rejeita, web+PDV) e a **comanda da cozinha** imprime as escolhas agrupadas. Lógica pura em `public/grupos.js` (testada). Inclui a **reversão da "grupos de opções v1"** (abordagem anterior, juntas no mesmo branch por decisão). **Correções no teste em produção:** (1) `estaAberto` não derruba mais o cardápio (500) com config sem bloco `atendimento`; (2) item **"só no local"** virou **só visualização** no cardápio web (abre pra ver, sem pedir); (3) **modal do item no mobile** com rodapé fixo (Adicionar sempre visível) e sem scroll horizontal; (4) controles obrigatório/mín/máx do editor **numa linha só**. **129/129 testes + `npm run check`.** UI validada no navegador (Playwright: modal mobile e editor) + **smoke check em produção (HTTP 200)**. Mergeado na `main` (PR #1) e **deployado no Fly** (no ar). — 2026-06-25
- [x] **Auditoria pré-produção — 6 correções P2** — fechados os itens de menor risco da auditoria: (1) **SSL explícito** no pool Postgres (`src/db.js`) — sem `sslmode` na URL o `pg` conectaria sem criptografia; forçado `ssl` (semântica `sslmode=require`); (2) **PDV entrega fora da área** deixou de zerar o frete em silêncio — servidor (`/api/pdv/vender`) responde **400** para `foraDaArea`/`incompleto` e o front (`pdvConfirmarEntrega`) **bloqueia** mantendo o overlay aberto (cortesia segue só dentro da área); doc `planos-e-frete.md` corrigida; (3) **`UNIQUE (empresa_id, numero)`** em `pedidos` (migration `20260625120000`) como rede de segurança contra duplicata de número (a corrida já estava serializada pelo lock FOR UPDATE) — **aplicada em produção** via `npx supabase db push` (o índice criou sem erro → sem duplicatas pré-existentes); (4) **`carregarPedidos`** com `try/catch` + toast (a aba mais usada não falha mais muda); (5) **acessibilidade dos modais do PDV** — `aria-modal="true"` + **foco inicial** no 1º campo (item/pagamento/entrega/desconto); (6) **`engines: node >=20`** no `package.json` (migrations antigas não-idempotentes mantidas — inócuas via Supabase CLI). **114/114 testes + `npm run check`.** Front (2,4,5) validado por revisão (sem harness logado). Docs (CHANGELOG `0.49.2`, modelo-dados, planos-e-frete) atualizadas. — 2026-06-25
- [x] **Auditoria pré-produção — P0 estoque atômico + 4 correções P1** — varredura de ponta a ponta antes do 1º cliente real (5 agentes em paralelo). **P0 (corrigido):** baixa de estoque era lida do cache, decrementada e regravada **fora de transação** → dois pedidos quase simultâneos (cardápio web + PDV) sobrescreviam o decremento um do outro (vendia sem estoque / contagem não batia). Agora `store.baixarEstoqueTx(client, dir, itens)` faz, **dentro da transação do pedido**, `SELECT cardapio … FOR UPDATE` (trava o tenant, serializa baixas e o `MAX(numero)+1`), revalida na versão fresca (`.code=ESTOQUE` → ROLLBACK) e decrementa; cache sincronizado após COMMIT. PDV (`caixa.venderLocal`) e cardápio web (`POST /api/c/:slug/pedido`, estoque insuficiente → 409) na mesma transação; `salvarPedido` aceita client opcional. Teste novo `test/store-estoque.test.js`. Commit `f771d1e`. **P1 (corrigidos):** (1) link de "esqueci a senha" deixou de confiar no header `Host` (forjável) e usa `PUBLIC_URL` (`src/servidor.js`); (2+3) reconexão do bot — `fechandoManual` volta a `false` no início de `conectar()`, timer de reconexão virou cancelável (`reconexaoTimer` + `clearTimeout` no `desconectar`) e o handler `connection.update` ignora eventos de socket antigo (`tt.sock !== sock`) → sem "bot offline silencioso" nem reconexão fantasma (`src/multi-bot.js`); (4) `venderLocal` revalida o caixa **dentro** da transação com `FOR UPDATE` (fecha a janela entre o check e o commit). **114/114 testes + `npm run check`.** Ressalva: P1 2–4 são lógica de integração (Baileys/Postgres) sem harness unitário — validado por suíte + revisão; comportamento sob carga a confirmar em staging/produção. Pendências da auditoria (P2: SSL Postgres explícito, frete fora-da-área silencioso no PDV, `carregarPedidos` sem `.catch`, modais PDV sem `role=dialog`/foco, `engines`) seguem em aberto. — 2026-06-25
- [x] **PDV — vendas no local (Plano Completo)** — nova aba **PDV** no painel para venda de balcão: grade de produtos (chips de categoria + busca) + carrinho; itens com **opcionais**, **observação** e por **kg** (mini-modal de peso/adicionais); tela de pagamento com **desconto** (R$/%), **pagamento dividido** (split), **troco** e **impressão ao finalizar**. Exige **caixa aberto** (banner "Abra o caixa" senão); a venda vira **pedido tipo "Balcão" recebido** + **1 movimento por forma** no caixa + **baixa de estoque**, tudo numa transação que **recalcula no servidor** (nunca confia no preço do cliente). Disponível só no Completo: gate `temPdv` (front+back/403), preset no upsell, card na landing, bullet no checkout. Backend: `src/pdv.js` puro (recalcular venda kg+opcionais, desconto, validar split, troco — **8 testes**), `caixa.venderLocal`, rota `POST /api/pdv/vender`, migration `pedidos.desconto`; `estoque.js` ficou **kg-aware** (baixa por peso). Docs: CLAUDE.md, modelo-dados, planos-e-frete; spec em `docs/superpowers/specs/`. **108/108 testes + check.** Validado no Playwright (servidor stub local, sem Supabase): gate/grade/busca, item simples, opcionais, kg, carrinho, desconto+split+troco, finalizar → pedido Balcão + impressão; desktop e mobile (carrinho vira folha). UI no painel logado real (com Supabase) não validada. — 2026-06-24
- [x] **Identidade visual do cardápio — capa (banner) + logo** — nova seção "Identidade visual" em Configurações → Empresa com upload de **capa** (banner largo) e **logo** (quadrada) + prévia WYSIWYG no painel; o cardápio web (`/c/:slug`) ganhou header estilo app de delivery: **capa full-width no topo + logo circular centralizada sobreposta** + nome/status abaixo, com fallbacks (gradiente da marca + inicial quando faltam imagens) e carrinho flutuante; responsivo. Disponível em todos os planos, sem migração (URLs em `config.restaurante.capa/logo`, reusa `POST /api/imagem`/Storage). **Inclui correções de UI no cardápio** (relatadas no celular após deploy): barra de categorias **sticky** agora gruda no topo (estava em `top:67px`, resíduo do header antigo) com fundo sólido/filete + folga p/ o carrinho no mobile; **emojis trocados por ícones SVG** (lixeira do carrinho, ✓ da tela de sucesso, ✓/⚠ do frete e do aviso de fechado) e os **× de fechar** (modal do item e carrinho) viraram ícone X SVG. Validado no Playwright (capa+logo, fallback, sticky ao rolar, ícones no modal/carrinho — mobile e desktop). 100/100 testes + check. — 2026-06-24
- [x] **Front — Daruma na Ajuda + rodapé legal no cardápio** — painel/Central de Ajuda passou a listar a Daruma DR700/DR800 nas impressoras suportadas (+ nota na FAQ de corte: o sistema envia o comando próprio automático, usar Parcial); cardápio web ganhou rodapé permanente com links para Política de Privacidade e Termos (antes só no checkout). Validado no Playwright. — 2026-06-24
- [x] **Adequação LGPD (Fases 1–4)** — fechadas as 10 lacunas do relatório `lgpd-checker` (de ❌ NÃO CONFORME/71 → ⚠️ PARCIALMENTE CONFORME/90, zero ALTO/CRÍTICO): aviso de privacidade + link à Política no checkout do cardápio (cliente final); e-mail do DPO (`atendimento@nymbuslab.com.br`) + prazo de 15 dias na Política; registro do aceite do dono no cadastro (`termos_aceitos_em`/`termos_versao`, exige `aceite`); Política com Resend/Geoapify + transferência internacional (banco nos EUA); docs em `docs/lgpd/` (ROPA, subprocessadores, resposta a incidentes, índice); trilha de auditoria (tabela `auditoria` + `src/auditoria.js`, registra conta criada/exportada/excluída). Pendências futuras (não bloqueiam) na Fase 5 de Próximos Passos. — 2026-06-24
- [x] **Cardápio web — vitrine de Destaques em carrossel** — a seção Destaques virou um carrossel horizontal de cards hero (imagem grande de fundo + nome/preço/Adicionar sobre a foto): no desktop com setas de navegação (mostra ~2 cards + o próximo espiando), no celular por swipe; pontinhos abaixo indicam a posição e que há mais (dot ativo acompanha o scroll, clicável). Mostra todos os destaques. Validado no Playwright (desktop + mobile). — 2026-06-24
- [x] **Cardápio web — cards de produto premium (vertical, imagem grande)** — os cards trocaram a lista horizontal por cards verticais com imagem grande no topo, selo flutuante sobre a foto (Destaque/Esgotado/Só no local/Pesado no balcão), nome, descrição, preço (com ícone de etiqueta) e botão Adicionar; 4 colunas no desktop e 2 no mobile, hover com elevação + glow, entrada em cascata. No mobile o preço fica centralizado e nunca quebra (rodapé empilha com Adicionar largura total). Placeholder com ícone quando sem foto. Validado no Playwright. — 2026-06-24
- [x] **Cupom com cara de recibo/marketing (cabeçalho + rodapé)** — o cupom do cliente ganhou cabeçalho com nome/endereço/telefone da empresa e rodapé com mensagem de "volte sempre" personalizável (campo novo em Configurações → Impressora; vazio usa um padrão) + chamada pro cardápio digital com o link do tenant. Cabeçalho refinado: CEP saiu da linha do endereço e foi pra **mesma linha do telefone**; data/hora no padrão **dd/mm/aaaa - HH:MM** (cupom e cozinha). Via cozinha intacta. Validado por render/teste (100/100); textarea da aba Impressora não validada no painel logado. — 2026-06-23
- [x] **Corte na impressora Daruma + avanço de papel** — o corte serial trocou do comando `GS V` (ignorado pela linha Daruma) para o legado Epson `ESC m` (picote) / `ESC i` (total), nativo na Daruma DR700/DR800 e aceito pela maioria das térmicas; avanço final de 3→6 linhas pra empurrar o fim do cupom pra fora da guilhotina. **Validado em hardware real (DR800) pelo usuário:** pica o papel e o cupom sai inteiro. 100/100 testes. — 2026-06-23
- [x] **Cardápio — bloco "disponibilidade + estoque" em faixa full-width no modal** — os 3 toggles (Disponível/Entrega/Destaque) e Unidade/Estoque/Estoque mínimo saíram da coluna estreita pra uma faixa de largura total abaixo da linha foto+campos: toggles deixam de quebrar (2+1 → 3 em linha), ocupam o espaço vazio sob a foto, colunas alinhadas; mobile empilha. Sem mudança de lógica/IDs. UI por harness/Playwright. — 2026-06-23
- [x] **Fix — piscada do modal ao marcar checkbox** — removida a única regra `:has(input:checked)` da folha de estilo (destaque do card de frete), que forçava recálculo do documento inteiro a cada toggle de checkbox (flicker no modal). Trocada por classe `.selecionado` em `renderEntregaModo` (mesmo destaque). Ressalva: não reproduzia no Chromium de teste, mas era o único gatilho global e a troca é segura. — 2026-06-23
- [x] **Upsell do Plano Completo — card reutilizável + controles bloqueados** — modal de upgrade genérico por presets (`UPSELL_FEATURES`: impressão/caixa/freteRaio); `abrirUpsell(key)` preenche ícone/título/benefícios. No Essencial: "Imprimir comanda" aparece bloqueado (cadeado) e abre o card; "Frete por raio" e os "Ver planos" de caixa/impressora também abrem o card. Nova feature Completo = só adicionar preset. Validado no app real. — 2026-06-23
- [x] **Assinatura — cortesia assina o Completo (autoatendimento) + customer Stripe obsoleto** — cortesia no Essencial ganhou "Assinar o Plano Completo" → checkout com Completo pré-selecionado (antes era beco sem saída); trial/ativo seguem com o upgrade normal. Fix: `garantirCustomer` se auto-recupera quando o `stripeCustomerId` salvo não existe no Stripe atual (id de outra conta/modo, live×test) — antes quebrava o checkout com "No such customer". Ressalva: checkout E2E depende do Stripe (validar logado). — 2026-06-23
- [x] **CI — testes verdes sem segredos** — workflow define `DATABASE_URL` dummy; `test/frete.test.js` importa `frete.js`→`db.js`, que lançava sem `DATABASE_URL` (sem `.env` no CI). Pool não conecta e nenhum teste consulta o banco → URL falsa basta. 95/95 com a dummy. — 2026-06-23
- [x] **Impressão via porta serial (COM) — Web Serial + corte configurável (Plano Completo)** — além da impressão pelo navegador (USB/driver, `window.print`, padrão), o painel passa a imprimir a comanda **direto numa térmica COM**, sem caixa de diálogo: encoder ESC/POS puro (`public/serial-escpos.js`, init+CP850+corte, testado), módulo Web Serial (`public/serial.js`: conectar/lembrar a porta/imprimir), config em **Configurações → Impressora** (método, baud, "sem acento", **Corte do papel: Parcial (picote)/Total/Não cortar**) e roteamento em `impressao.js` (serial quando configurado/suportado; senão cai no navegador). **Corte parcial virou o padrão** — térmicas baratas ignoram o corte total e o papel não cortava. USB segue intacto. **95/95 testes + check.** Serial **validado em hardware real pelo usuário** (imprime direto); o corte parcial é a correção recém-enviada, a confirmar na próxima impressão. — 2026-06-22
- [x] **Central de Ajuda (FAQ) no painel** — botão **Ajuda** no rodapé da sidebar (acima do Sair) + atalho no topo (mobile, que oculta o rodapé) abrem uma **página de FAQ** dentro do painel: acordeão nativo (`<details>`, CSP-safe, acessível) ancorado no design system. Primeira categoria **Impressão de comandas** com o manual completo: impressoras suportadas, USB × serial, corte do papel, conectar serial (COM) e **imprimir direto pelo USB via `--kiosk-printing`** (passo a passo). Estrutura pronta pra novas categorias. UI validada por harness/Playwright (não no painel logado). — 2026-06-22
- [x] **Cardápio — produto em destaque** — checkbox "Destaque no cardápio" no modal; selo estrela na tabela do painel; na vitrine, **seção "Destaques" no topo** da aba Todos + selo no card (item também segue na categoria). Projeção expõe `destaque` (testado). Inclui o ajuste de layout dos toggles do modal (grade 2 colunas que adapta p/ 1 no mobile). Sem migration. UI por harness/Playwright. — 2026-06-22
- [x] **Cardápio — unidade de medida un/kg (etapa 3b)** — seletor Unidade (un/kg) no modal; item kg aceita estoque decimal, mostra "R$ X/kg", estoque/mínimo em kg, vendido só no local (vitrine: "Pesado no balcão", **não-adicionável**); servidor rejeita kg em pedido e a projeção expõe `unidade`. Helper `formatarQtd` + `statusEstoque` por unidade. Campo `unidade`, sem migration. **89/89 testes**; UI por harness/Playwright. Fecha as 3 etapas do cardápio. — 2026-06-22
- [x] **Cardápio — tabela de produtos + arquivar/excluir item com vendas** — Gestão de Itens virou tabela com colunas (Produto·Preço·Estoque·Mínimo·Disponível·Ações), agrupada por categoria, alinhada à esquerda; cartões com rótulos no mobile. Exclusão segura (padrão de mercado): servidor checa vendas (`GET /api/cardapio/item/:id/vendas`, containment no `pedidos.itens`); sem vendas → confirmação; com vendas → modal recomendando Arquivar (preserva histórico/estoque) ou "Excluir mesmo assim". Arquivado sai da vitrine e não é pedível; "Mostrar arquivados" + Restaurar; salvar na hora com rollback. UI validada por harness/Playwright. **Ressalva: fluxo dinâmico (vendas→modal→salvar) + query de containment pendentes de teste com sessão logada + DB real.** — 2026-06-22
- [x] **Cardápio — estoque ativo (etapa 3a)** — campos Estoque/Estoque mínimo no modal (vazio = ilimitado); selos Esgotado/Baixo/Est. na lista; vitrine mostra "Esgotado" e bloqueia adicionar (sem vazar contagem); servidor valida (esgotado/over-order "Restam só N…") e dá baixa a cada pedido (trava em 0, best-effort). Helpers puros `public/estoque.js`. Limitações: concorrência last-write-wins, sem reserva no carrinho (ok p/ 1 restaurante). — 2026-06-22
- [x] **Cardápio — item "só no local" (etapa 2)** — toggle "Disponível para entrega" no modal; selo "Só no local" na vitrine (segue adicionável) + tag na lista; no checkout, com item só-local no carrinho a Entrega desabilita e Retirada assume; servidor rejeita Entrega com item só-local (helper puro `itensSoLocal`, testado). Campo `apenasLocal`, sem migration. — 2026-06-21
- [x] **Cardápio em lista com busca (painel do dono)** — a "Gestão de Itens" trocou a grade de cards por uma **lista agrupada por categoria** (miniatura · nome/descrição · preço · toggle Disponível · editar · excluir; indisponível esmaecido), com **busca por nome no topo** (tempo real, sem acento — "cafe" acha "Café", categoria sem match some, `Esc` limpa, estado vazio quando não encontra, foco preservado ao digitar). Responsivo (empilha no mobile, sem scroll-x). Helper puro `public/busca.js` (`normalizarTexto`/`itemCasaBusca`, +5 testes); render e wiring em `public/app.js`/`admin.html`; CSS novo + remoção do CSS morto dos cards. Não tocou o modal, a vitrine pública nem o fluxo de pedido. **Etapa 1 de 3** do cardápio (faltam Entrega/Apenas local e Estoque+unidade). **71/71 testes + `npm run check`**; UI validada por harness + Playwright (desktop/mobile). — 2026-06-21
- [x] **Caixa do dia — tela estilo PDV, extrato, regra de recebimento e Caixas anteriores (Plano Completo)** — em cima do fechamento-contador (entrada abaixo): **não fecha com vendas do turno a receber** (banner + bloqueio + atalho pros pedidos); o **relatório de fechamento passou a ser montado no servidor** (fonte única; correção de XSS por não confiar em conteúdo do cliente); **tela do caixa aberto repaginada estilo PDV** (Total em Caixa em destaque, cards **Vendas por forma** — lista todas as formas configuradas, zeradas se sem venda — e **Movimentação do caixa** com box Total Faturamento) + **extrato do turno** em tabela (Hora/Nº/Tipo/Cliente/Valor/Forma, sangria destacada, **Estornar** nos recebimentos); **Caixas anteriores** com resumo por linha (operador · Total em Caixa · Fechado · diferença), só os **3 últimos**, clicável p/ **reabrir o relatório**, com **toggle** (abre/fecha) e entrada suave; lançar cartão/Pix com **Enter** (mantém o foco); **gate do caixa autoritativo** (decide pela resposta da API → sem cadeado falso na navegação inicial); correção do **Pix por união de formas** (não cai mais em "Outros"); **data/hora** em cada recebimento; ajustes de layout (espaçamento/padding/compactação) validados por harness + screenshot (desktop e mobile). `listarCaixas` devolve operador/totais. **66/66 testes + `npm run check`**. (build OK, UI não validada por ferramenta — só harness/screenshot). — 2026-06-21
- [x] **Fechamento de caixa — contador de cédulas + conferência de cartão/pix + relatório 80mm (Plano Completo)** — o fechamento deixou de ser um modal de 1 campo e virou uma **tela de conferência** (substitui `fecharCaixaUI` por `renderFechamentoCaixa`): à esquerda um **contador de 12 cédulas/moedas** (R$200 → R$0,05) com total e **diferença ao vivo** vs o esperado em espécie; à direita **lançamentos de cartão/pix** por **forma configurada** (`config.pagamentos`, adicionar/remover, soma + diferença vs esperado eletrônico). Ao fechar, abre a **prévia 80mm** de um **relatório agregado** (VENDAS por forma · Saldo Inicial/Suprimento/Retirada · Total de Vendas / **Total em Caixa** = saldo+suprimento+vendas−sangria · **FECHAMENTO OPERADOR** · diferença global SOBROU/FALTOU). Cálculos PUROS em `src/caixa-calc.js` (`totalContagem`/`esperadoEletronico`/`totalEmCaixa`, +4 testes) e relatório PURO dual-mode em `public/relatorio-caixa.js` (+5 testes); backend **recalcula no servidor** a partir do detalhamento (`src/caixa.js` `fecharCaixa`), migration `20260620140000` adiciona `contado_eletronico`+`detalhe_fechamento` (jsonb) e `diferenca` passa a ser **global**. `GET /api/caixa` expõe `formasPagamento`+`restaurante`. Gate `temCaixa` mantido. **66/66 testes + `npm run check`**; **UI não validada visualmente** (exige sessão Completo + caixa aberto). Granularidade Crédito/Débito + **taxa por forma (recebimento líquido)** foram separadas como feature futura no ROADMAP P3. Spec/plano em `docs/superpowers/`. — 2026-06-20
- [x] **Abertura de caixa — tela redesenhada (Plano Completo)** — a "abrir caixa" virou a tela do protótipo: badge com ícone, título/subtítulo, **Operador** (pré-preenchido com o nome do painel, editável), **Saldo inicial** com prefixo `R$` (máscara `dinheiro.js`) e **Observações**. Backend grava `operador`+`obs_abertura` (migration `20260620130000`); `abrirCaixa`/rota/`resumo` atualizados. CSS com os tokens existentes. — 2026-06-20
- [x] **Polimento de UX — impressão por prévia, recebimento no pedido, ícones e modais** — refina o Caixa e a Impressão (entradas abaixo). **Impressão:** "Imprimir comanda" abre um **modal de pré-visualização** das 2 vias (Imprimir cozinha/Imprimir cupom; cada via = 1 impressão, a térmica corta no fim de cada) — **removido o toggle "cortar entre as vias"**; prévia fiel ao papel (lado-a-lado, largura ~48 colunas, sem quebra falsa; endereço longo quebra como no 80mm). **Caixa:** o **recebimento passou a ser só no Pedido** (selo "A receber"/"Recebido" + filtro na aba Pedidos, só no Completo); o Caixa ficou enxuto (abrir/sangria/suprimento/fechar/histórico + "Recebimentos deste caixa" com **estorno**). **Sangria/suprimento/fechar** viraram **modal acessível** (máscara monetária, foco/Esc/Enter, **diferença ao vivo** no fechar). **Emojis → ícones Lucide** (sino, cadeado, tags entrega/retirada, map-pin), mantendo 👋 dos templates de WhatsApp. **Landing** reposicionada "do WhatsApp à cozinha" + impressão como benefício do Completo. Fixes de layout (rodapé/selo do modal do pedido; prévia lado-a-lado). Validado no navegador (Playwright, servidor só-Express, sem tocar produção). CHANGELOG `0.31.0`/`0.32.0`. — 2026-06-20
- [x] **Caixa do dia / fechamento (Plano Completo)** — nova aba **Caixa** no painel: abrir com **fundo de troco**, **recebimento por pedido** (forma/valor pré-preenchidos, estornável — pedido do WhatsApp nasce "a receber" via nova coluna `pedidos.recebido_em`), **sangria/suprimento** (só dinheiro), e **fechamento com conferência** (esperado em espécie = fundo + recebido em dinheiro + suprimentos − sangrias → conta a gaveta → **diferença** sobra/falta) + **histórico**. Só **"Dinheiro"** (case-insensitive) entra na conferência física; Pix/cartão só no relatório. **1 caixa aberto por vez** (índice único parcial). Tabelas `caixas`/`caixa_movimentos`; cálculos PUROS testados em `src/caixa-calc.js` (+ `test/caixa-calc.test.js`, 4 casos); orquestração em `src/caixa.js`; gate **`temCaixa` no front E no backend (403)** — caixa é recurso de servidor. Botão **Receber** também no modal do pedido. **Validado E2E** (servidor local só-Express, sem bot, p/ não tocar a produção): API completa (abrir / dupla-abertura bloqueada / suprimento+sangria → esperado 85 / receber Dinheiro 50 → 135 / receber repetido bloqueado / estorno → 85 / fechar contado 90 → **diferença +5** / histórico) + **UI no navegador (Playwright)** (aba, gate Completo, abrir, painel com 6 pedidos "a receber"); dados de teste **limpos do tenant real** ao fim. 57/57 testes + `node --check`. CHANGELOG `0.30.0`. Spec/plano em `docs/superpowers/`. — 2026-06-20
- [x] **Impressão de pedido na térmica 80mm (Plano Completo)** — botão **🖨️ Imprimir comanda** no modal de detalhe do pedido e no modal de novo pedido (gated por `planoAtual === "completo"`). Imprime **2 vias** pelo navegador (`window.print()` + CSS `@page 80mm`, `font-size:2.5mm` p/ 48 colunas caberem sem quebra): **cozinha** (itens/opcionais/observações, **sem preços**) + **cupom** (cliente/endereço/pagamento/total). Lógica de montagem PURA e testada (`public/comanda.js` dual-mode Node/browser + `test/comanda.test.js`, 5 casos); orquestração em `public/impressao.js`; nova **sub-aba Configurações → Impressora** com o toggle **"cortar entre as vias"** (`config.impressao.cortarEntreVias`, jsonb via `PUT /api/config` — sem migração; desligado=1 trabalho/vias juntas, ligado=2 trabalhos/guilhotina corta entre elas). Compatível com qualquer térmica com driver (Elgin i7/i8, Epson T20x…). **Validado:** 51/51 testes + `node --check`; **pipeline real validado no browser (Playwright)** — vias juntas=1 trabalho c/ tracejado, cortar=2 trabalhos (cozinha→cupom), layout 80mm sem quebra (screenshot). UI no painel logado não validada visualmente (exige sessão Completo + Supabase). Caminho ESC/POS (QZ Tray, corte fino/silencioso, sem painel aberto) fica como futuro. CHANGELOG `0.29.0`. Spec/plano em `docs/superpowers/`. — 2026-06-20
- [x] **Resend Fase A — "esqueci a senha" + e-mails transacionais** — módulo `src/email.js` (envio via Resend por fetch, templates com a marca; no-op sem `RESEND_API_KEY`). **"Esqueci a senha"** (cliente e master, ambos no Supabase): `POST /api/esqueci-senha` (genérico/anti-enumeração, token hasheado 1h em `password_resets`, link via Resend) + `POST /api/redefinir-senha` (valida token → `updateUserById`) + link no login + página `/redefinir-senha`. **Transacionais** (fire-and-forget): boas-vindas (cadastro), assinatura confirmada (confirmar), aviso de troca de senha/e-mail (cliente e master), cancelamento (webhook `subscription.deleted`), exclusão de conta. Env `EMAIL_FROM`. Validado em prod: envio Resend (200+id), "esqueci a senha" (real/anti-enumeração/token inválido) e boas-vindas E2E (cadastro de teste + limpeza). CHANGELOG `0.28.0`. E-mail **marketing** fica **fora de escopo** aqui — será tratado de forma independente depois. — 2026-06-19
- [x] **Master migrado para o Supabase Auth (allowlist)** — o super-admin deixou de ser env+hash (`SUPERADMIN_SENHA_HASH`/`tokensAdmin`/`verificarSenhaMaster`) e virou **usuário do Supabase Auth**; vira super-admin por **allowlist de e-mail** (`plataforma_config.master_email` com bootstrap em `SUPERADMIN_EMAIL`). Login = `signInWithPassword` + checa e-mail == master → JWT do Supabase (+ refresh); `exigeSuperAdmin` valida o JWT (jose/JWKS via `empresas.emailDoToken`) + allowlist; `POST /api/admin/refresh` renova; `PATCH /api/admin/conta` troca e-mail/senha via Supabase. Front (login.js/app-admin.js) guarda token+refresh e renova no 401. Ganha "esqueci a senha"/MFA do Supabase (a tela de reset vem na Fase 2). Limpeza: removidos `hashSenha`/`verificarSenhaMaster`, `scripts/gerar-hash.js`, `npm run gerar-hash-admin`, env `SUPERADMIN_SENHA_HASH`. **Validado E2E em produção:** login master OK, master acessa `/api/admin/*`, **restaurante é bloqueado (401)**, refresh OK, senha errada 401. Usuário master criado (`pabllomoliveiraa@gmail.com`). — 2026-06-19
- [x] **Planos (Essencial R$ 79 × Completo R$ 99) + Frete por raio** — feature completa (Partes 1–4; spec em [docs/planos-e-frete.md](docs/planos-e-frete.md)): infra de planos com gating (`temFreteRaio`); aba **Entrega** (frete fixo movido + modo "por raio" com cadeado pro Essencial); **frete por raio** (Geoapify + Haversine + faixas por km, cálculo no checkout pelo CEP, recálculo no servidor, fora-da-área→retirada); **escolha de plano no checkout** + **upgrade/downgrade** na Assinatura (proration) + dois planos na landing. No ar (CHANGELOG `0.27.0`). **Frete por raio validado E2E em produção** (2026-06-19): geocode da empresa no save + ViaCEP→Geoapify→Haversine→faixa com distâncias reais (perto 0,53km→R$5 · médio 4,13km→R$8 · longe 20,43km→fora da área) + projeção sem vazar faixas; config revertida ao fim. **Pendente:** teste do Stripe com cartão real (item standing). — 2026-06-19
- [x] **Painel master: trocar o plano do cliente (Essencial ↔ Completo)** — no modal Gerenciar do `/admin-master`, mostra o **plano atual** + botão "Mudar para Plano …". Backend `PATCH /api/admin/tenants/:slug/plano` (sob `exigeSuperAdmin`): com assinatura viva no Stripe → `stripe.trocarPlano` (proration); cortesia/sem Stripe → ajusta a coluna `plano` (override). `GET .../assinatura` passou a devolver `plano`. Permite reverter o `sabor-d-casa` pela tela. Validado: sintaxe + 50/50; E2E após deploy. — 2026-06-19
- [x] **Fix: aba Pedidos — "NOVO" não sumia ao clicar + lista mais rápida** — (1) clicar num pedido removia o destaque só ao **sair** da aba; agora o clique tira o "NOVO" do pedido na hora (`pedidosNovosDestaque.delete` + re-render). (2) Diagnóstico do "atraso no Hoje": **não era bug de fuso nem por filtro** (os 3 filtros leem a mesma lista; o pedido era "hoje" no fuso BR) — era a latência do **poll de 15s**. Baixado pra **6s** → pedido novo aparece em ~6s em qualquer filtro (instantâneo de verdade exigiria push/websocket — item à parte). — 2026-06-19
- [x] **Fix: checkout não abria (loop pisca-tela) após cadastro** — `checkout.js` ainda lia o access token do `sessionStorage`, removido na migração de sessão segura (v0.26.0; token agora só em memória, via cookie httpOnly). Em página nova o token vinha vazio → `checkout.html` → `login.html` → auto-login → painel (o "pisca"). Corrigido espelhando o `app.js`: boot via `POST /api/refresh` pega o token do cookie antes de montar o checkout. Bug de produção (bloqueava qualquer assinatura). Validado: sintaxe; E2E após deploy. — 2026-06-19
- [x] **Restaurar o bot no boot (P1)** — depois de um deploy/restart, os bots conectados voltam **sozinhos, sem QR**, em vez de ficarem offline até reconexão manual. `wa-auth.slugsComSessao()` lista os tenants com credencial salva (`chave = 'creds'` → já conectaram antes → reconectam sem QR); job `restaurarBots()` no [index.js](index.js) (10s após o boot, 1x) cruza esses slugs com `empresas.listar()` filtrando por **`acessoLiberado`** (ativo + assinatura trialing/active/cortesia) e chama `multiBot.iniciar(slug, tenantDir(slug))` espaçado ~1,5s. Suspenso/vencido ou quem nunca conectou **não** reconecta (sem QR fantasma). Assume **instância única** (anotado no código: 2+ instâncias → `connectionReplaced`). Validado: `npm run check` (20 arquivos) + 34/34 testes; reconexão sem QR já comprovada em produção (v0.17.0) → prova final é o log do próximo boot (`🔌 Restaurando…` + `🤖 Bot ONLINE`). — 2026-06-18
- [x] **Auto-login: logado + "lembrar" cai direto no painel (login.html e landing)** — `login.html` e a landing (`/`) não checavam a sessão ao carregar, então quem estava logado (cookie de "manter conectado" válido) via o formulário/landing em vez de cair no painel. A auth por cookie **já estava no ar** (não era deploy) — só faltava o redirect nessas duas entradas. Como os cookies `rt`/`rtl` são httpOnly (o JS não lê), criado um cookie de **presença** `sess=1` (NÃO-httpOnly, `path=/`, validade espelha o "lembrar": persistente 30d / cookie de sessão), setado no `setSessaoCookies` e limpo no `limparSessaoCookies` → cobre login, rotação no refresh, logout, refresh-fail e exclusão de conta automaticamente; não guarda nada sensível (o refresh token segue httpOnly). No front (`login.js`/`landing.js`): se `sess` existe → `POST /api/refresh`; **200** → `location.replace("admin.html")` (login.html esconde o body enquanto checa, sem piscar); **401** → o servidor limpa os cookies e a página aparece normal (sem loop). Visitante anônimo não dispara `/api/refresh` (sem `sess`). Validado E2E (Playwright + tenant descartável criado/excluído + conta real): anônimo vê form/landing; logado redireciona de login.html e `/` pro painel; logout limpa `sess` e tudo volta ao normal; `sess` órfão → form visível, sem loop, servidor limpa o cookie. Set-Cookie conferido (persistente com lembrar / sessão sem). ⚠️ exige `fly deploy`. **Auto-redirect espelha o login:** `/api/refresh` passou a devolver `onboardingConcluido` (`empresas.renovarSessao` lê `config->'onboarding'`, ausente = concluído), e `login.js`/`landing.js` mandam pra `cadastro.html` quando incompleto, senão `admin.html`. Validado E2E nos dois caminhos (tenant incompleto → cadastro.html; conta real completa → admin.html). — 2026-06-18
- [x] **Cardápio web — categorias visíveis + aba "Todos"** — no `/c/:slug` a categoria não aparecia em lugar nenhum quando o restaurante tinha 1 só (caso do Sabor D'Casa: 1 categoria "Espetos"): a barra de chips era escondida com `≤1` categoria e o título de seção só renderizava com 2+ grupos. Em `public/cardapio.js`: (1) título da seção passa a renderizar sempre que o grupo tem nome (`if (g.nome)`), (2) nova aba **"Todos"** (`catAtiva === null`, ativa por padrão) que empilha todas as categorias com seus títulos, (3) a barra de categorias aparece sempre que houver ≥1 categoria, montando **[ Todos ] [ cat… ]**. Busca segue sem título (grupo sem nome) — sem regressão. Validado E2E (Playwright local, Sabor D'Casa real): Todos mostra "ESPETOS" + 3 itens, chip da categoria filtra, busca "queijo" filtra sem título; 0 erro de console (fora favicon). ⚠️ exige `fly deploy`. — 2026-06-18
- [x] **"Manter conectado" + auth segura por cookie httpOnly** — substituído o token no `sessionStorage` pelo padrão seguro de SPA: **refresh token em cookie `httpOnly` + `Secure` + `SameSite=Lax`** (o JS não lê → imune a XSS) e **access token só na memória** (nunca persistido). Checkbox **"Manter conectado neste dispositivo"** no login controla a validade do cookie: marcado → **persistente 30 dias** (reabre o navegador e cai direto no painel); desmarcado → **cookie de sessão** (some ao fechar). Backend (`servidor.js`): `/api/login` seta os cookies (`rt`+`rtl`), `/api/refresh` lê do cookie e **rotaciona**, `/api/logout` e exclusão de conta limpam; `empresas.renovarSessao` passou a devolver slug+nome e checar `ativo`. Front: `app.js` faz **boot via `/api/refresh`** (token em memória, `iniciarSessao` antes de `inicial()`); `login.js`/`cadastro.js` não guardam mais token. **CSRF mitigado** (SameSite=Lax + auth real das rotas via `Bearer`, não via cookie). Validado E2E (Playwright + curl): cookie httpOnly **invisível ao JS**, **nada** em storage, login→reload mantém a sessão, logout limpa e bloqueia acesso, sem cookie→login; lembrar=true→Max-Age 30d, lembrar=false→cookie de sessão, `/api/refresh` rotaciona/401. 34/34 testes. ⚠️ exige `fly deploy`. — 2026-06-18
- [x] **Notificação de pedido novo no painel (som + badge + modal rico)** — o painel avisa o restaurante quando chega pedido: poll leve a cada 15s (`GET /api/pedidos/ultimo` → `pedidos.ultimo`: nº, cliente, itens e total) detecta nº maior que o visto → **toca som** (`public/assets/notificacao-pedido.mp3`, com destrava de autoplay no 1º clique + toggle 🔔/🔕 no header, persistido no localStorage), mostra **badge** de contagem no item Pedidos da sidebar e, **fora** da aba Pedidos, abre um **modal dedicado** "Novo Pedido Recebido!" (fiel ao protótipo do usuário: ícone, pill de som que reflete o estado, bloco cliente/nº, **lista de itens com preço**, caixa de Valor Total, botões **Visualizar Pedido** + **Fechar**). "Visualizar Pedido" → aba Pedidos + abre o detalhe. Adaptação honesta: sem "Recusar" (ciclo do pedido é fora de escopo → botão é "Fechar"); subtítulo neutro. **Na** aba Pedidos: só som + tag **"NOVO"** na linha (sem modal). `ultimoNumeroVisto` por slug no localStorage (não alerta pedidos antigos; baseline na 1ª checagem). Som movido p/ `public/assets/`. **Validado E2E no navegador (Playwright, conta real Sabor D'Casa, sem escrever no banco — mock do fetch):** estado inicial, modal com todos os campos (João Silva/#4829/2× Burger R$65,90/1× Coca R$20,00/Total R$85,90), "Visualizar Pedido" abre o detalhe, toggle de som, badge, tag NOVO; produção intacta (3 pedidos). 34/34 testes unit. ⚠️ exige `fly deploy`. — 2026-06-18
- [x] **Limpeza ativa de sessões abandonadas (bot)** — a expiração de sessão era só **lazy** (`getSessao` só apagava a sessão de 30min quando a MESMA chave voltava); conversa abandonada nunca mais era acessada → ficava na RAM pra sempre (vaza memória na instância única com vários tenants). Adicionado `sessoes.limparExpiradas(agora?)` que varre o Map e descarta inativas há +`TEMPO_EXPIRA_MS` (deletar do Map na iteração é seguro em JS), agendado a cada **10min** no `index.js` (`limparSessoesMemoria`, com log quando remove). Sem mudança de comportamento (sessão ativa intacta; quem volta após 30min recebe sessão nova, como já era). Validado: 3 testes novos em `test/sessoes.test.js` (não remove dentro da janela / remove além de 30min / recria zerada após varrer) — npm test 34/34, check OK. — 2026-06-18
- [x] **Painel parava de deslogar sozinho (renovação de sessão)** — o JWT do Supabase expira em ~1h e o front nunca renovava → a cada hora a próxima chamada dava 401 e `api()` jogava o usuário pro login ([app.js:32-34](public/app.js)). Agora o login guarda também o `refresh_token` (antes descartado em `empresas.autenticar`); nova rota `POST /api/refresh` (+ `empresas.renovarSessao` → `supabaseAnon.auth.refreshSession`, com `refreshLimiter` 60/15min) troca o refresh por um novo par de tokens. No front, `api()` ao tomar 401 **renova uma vez e repete a requisição** (renovações concorrentes coalescidas num só refresh, pois o refresh_token rotaciona); só vai pro login se a renovação falhar. Sessão deixa de morrer a cada hora. `refresh_token` no `sessionStorage` (mesma superfície do token atual; cookie httpOnly fica como hardening futuro). Validado: 31/31 testes + sintaxe front/back. **UI não validada visualmente** (exige login real). No ar na v28 — 2026-06-18. (O bot parar junto é outra causa — restart do servidor sem restore no boot; em Próximos Passos.)
- [x] **Link do cardápio LIMPO (sem token) + secrets do cardápio web em produção** — (1) **Secrets no Fly:** `PUBLIC_URL=https://pedidos.nymbuslab.com.br` + `CARDAPIO_LINK_SECRET` (aleatório) setados — antes ausentes, o bot caía no aviso "cardápio quase pronto" em vez do link. (2) **Link limpo:** `linkCardapio` (`fluxo.js`) parou de anexar o `?p=<token>` — o link vira `/c/:slug` puro. A confirmação automática do pedido usa o **telefone (WhatsApp) digitado no checkout** (campo já obrigatório em `cardapio.js`); o fallback por telefone já existia em `confirmarPedidoWeb` (`servidor.js`). Motivo: pro caso `@lid` o WhatsApp **não expõe** o número na mensagem que chega, então captar do bot não é confiável — mas o checkout já coleta o número de qualquer forma. Trade-off: se o cliente digitar número errado, a confirmação não chega (pedido é salvo mesmo assim). Backend ainda aceita `?p=` de links antigos; `cardapioWeb` (import morto) removido de `fluxo.js`. Validado: check OK, 31/31 testes, preview do link limpo. No ar na v28 — 2026-06-18
- [x] **Fix do horário (fuso BR) + virada de noite + msg de fechado curta** — `estaAberto` (`fluxo.js`) calculava com a hora do **servidor** (UTC no Fly → 3h adiantado) e marcava "fechado" na hora errada (sobretudo de madrugada; ex.: quinta 00:00–02:00 BR = 03:00–05:00 UTC). Agora usa **America/Sao_Paulo** (`agoraBR`, mesmo padrão das métricas) e trata **janelas que viram a noite** (`fecha ≤ abre`; `00:00` = meia-noite/1440), incluindo a **cauda da madrugada** do dia anterior (ex.: Sexta 08:00→02:00 abre na noite e segue até 02:00 de sábado; Quarta 11:00→00:00 deixava o dia sempre fechado). Nova variável **`{proximaAbertura}`** ("hoje às 18:00" / "amanhã (sexta) às 08:00") p/ o usuário montar uma mensagem curta; `{horario}` (semana toda) mantido. Texto **padrão** de fechado encurtado (só tenants novos — não sobrescreve quem personalizou). Painel: legenda de variáveis reescrita em lista (`.cfg-vars`) + dica no campo "Aviso de Fechado". Validado: 12 casos da lógica com a agenda real (0 falhas) + `agoraBR` confirmando o offset; npm test 31/31, check OK. **UI não validada visualmente** (exige sessão logada). No ar na v28 — 2026-06-18
- [x] **Auditoria + teste E2E de isolamento multi-tenant** — verificado que as mudanças desta sessão (renovação de sessão, link limpo, fuso) **não quebraram o isolamento**: a fronteira (`exigeAuth` → `resolverPorToken`: JWT `sub` → `empresas WHERE user_id`) não foi tocada, e `/api/refresh` devolve só tokens (sem dado de tenant). Teste E2E ao vivo com **2 empresas fictícias** (criadas e excluídas): **25/25 checks** — token e **token renovado** sempre resolvem para o próprio tenant e nunca para o outro; `GET /api/config` por HTTP (app no ar) só lê os próprios dados; banco confirma mapeamento `user_id→empresa` 1:1 e pedidos particionados por `empresa_id`. — 2026-06-18

### Pacote de melhorias — UI/UX e conta (jun/2026)

- [x] **T1 — Fix barra de rolagem no horário (wizard)** — `.hor-time` do wizard passou de 118px (que estourava o card e criava scroll-x) para 100px + padding reduzido; cabe sem cortar a hora nem gerar barra. Validado (Playwright: scrollW == clientW, "22:00" inteiro)
- [x] **T2 — Bug: aba Assinatura não reflete `cortesia`** — `renderAssinatura` ganhou o status `cortesia` (badge roxo "Cortesia" + texto "acesso liberado pela equipe, sem cobrança" + sem botão de pagamento, que dava erro pois cortesia não tem customer Stripe). Card realinhado: cabeçalho com plano + preço (R$ 79/mês) e badge à direita. Validado (Playwright: cortesia/ativa/trial)
- [x] **T3 — Configurações em abas (Empresa × Bot)** — sub-abas internas na tela Configurações (sem mexer no menu lateral): **Empresa** (Dados do Restaurante + nova seção **Conta de acesso**: e-mail exibido, **Alterar e-mail** e **Alterar senha**, ambos pedindo a senha atual) × **Bot** (status de atendimento, horários + texto auto, mensagens, entrega/pagamento, comportamento). Backend novo: `GET /api/conta`, `PATCH /api/conta/senha`, `PATCH /api/conta/email` (valida senha atual via `signInWithPassword`, aplica via `admin.updateUserById`; e-mail também sincroniza a coluna `empresas.email`). `empresas.trocarSenha`/`trocarEmail`. Validado (Playwright: troca de sub-aba, mismatch de senha, sucesso de senha/e-mail; rotas reais → 401 sem token)
- [x] **T4 — Gestão de cartões na assinatura** — seção "Forma de pagamento" na aba Assinatura: lista os cartões (bandeira + •••• últimos4 + validade + selo "Padrão"), botão **Adicionar cartão** (Payment Element no mesmo visual escuro Nymbus, num modal), **Tornar padrão** e **Remover** (travado no cartão padrão e no único). Só aparece com Customer no Stripe (cortesia/sem assinatura → oculto). Backend (`src/stripe.js` + rotas): `GET /api/assinatura/cartoes`, `POST .../cartoes/setup-intent`, `PATCH .../cartoes/:id/padrao` (atualiza Customer + subscription), `DELETE .../cartoes/:id`. `admin.html` carrega `js.stripe.com/v3`. Validado (Playwright com stub do Stripe.js: listar, tornar padrão, remover c/ confirmação, adicionar; rotas reais → 401)
- [x] **T5 — Polimento de UI (mais bonita)** (P1) — passada de design, 1 tela por vez (T5a + T5b + T5c todos concluídos):
  - [x] **T5a — Landing** — refinamento sobre a base existente: animações de entrada ao rolar (`.lp-reveal` via IntersectionObserver, respeita `prefers-reduced-motion`), rótulos de seção (eyebrow "Vantagens/Como funciona/Plano único"), **moldura gradiente roxo→ciano** no card de preço (foco), cartão flutuante "Pedido #042 confirmado" sobre o mockup + indicador "digitando…" no chat, glow no botão primário e no ícone dos cards (hover), separadores na faixa de números. Sem cores novas, sem dado falso. Validado (screenshots desktop + mobile)
  - [x] **T5b — Painel do usuário** — passe de polimento coeso (skill `frontend-design` como guia, ancorado no design system Nymbus): **entrada em cascata** ao trocar de aba (`.aba.ativa > *` com `nth-child` delays, respeita `prefers-reduced-motion`), **indicador de ativo na sidebar** (barra de acento à esquerda no desktop, barra no topo no bottom-nav mobile), **hover/lift** nos cards de métrica, **hover de linha** na tabela de pedidos (acento à esquerda), e **chip "X dias de teste"** no header durante o trial (atalho pra aba Assinatura). Sem fonte/cor nova, sem mexer em layout/lógica. Validado (screenshots das 6 telas + mobile)
  - [x] **T5c — Painel master** — redesenho do Dashboard no visual do protótipo (dados reais): header com título + subtítulo + **Exportar** (CSV real de clientes), **4 cards hero** com ícone (Clientes ativos · Em teste · Assinantes · Cancelados de `/api/admin/metrics`), faixa secundária (Cortesia/Em atraso/Pedidos no mês/Conectados), e **"Últimos Clientes Cadastrados"** (top 5 por data, avatar de iniciais, badge de status, ação olho → Gerenciar). Menu renomeado pra **Dashboard · Clientes · Configurações Master**. Nova aba **Configurações Master** (base da futura aba Nymbus): edita o **WhatsApp de suporte** (tabela singleton `plataforma_config`, migration aplicada; `GET/PUT /api/admin/plataforma`) que alimenta o "Falar com Suporte" do cliente (`GET /api/plataforma` lê DB→env). Adaptações honestas: sem setas de tendência (não há histórico), avatar neutro (sem foto), sem seletor de data, marca Nymbus. Validado (Playwright dashboard/config/mobile + teste real do módulo no banco)
- [x] **Redesenho da aba Assinatura (visual do protótipo)** — página de billing rica, fiel aos protótipos enviados pelo usuário, **adaptada a dados reais**: card largo do plano (PLANO ATUAL `Plano Nymbus Pedidos` · VALOR `R$ 79,00/mês` · PRÓXIMO VENCIMENTO · badge de status) com moldura gradiente; duas colunas (Pagamento + "Precisa de ajuda?" à esquerda, **Histórico de Faturas** à direita). Faturas **reais do Stripe** (`listarFaturas` ligado ao `GET /api/assinatura`): data, valor, status (Pago/Em aberto/…), **download de PDF**; estado vazio honesto sem faturas. "Falar com Suporte" → WhatsApp via `GET /api/plataforma` (env `SUPORTE_WHATSAPP`; futura aba "Nymbus" do master alimentará); card oculto sem número. Mobile empilha (faturas viram cards com `data-label`). Não incluídos: sino de notificações e rodapé Privacidade/Termos (recursos inexistentes). Validado (Playwright: active c/ faturas, cortesia, mobile)
- [x] **T6 — Auditoria de responsividade** — varredura de overflow horizontal (sinal preciso de quebra) em TODAS as telas (landing, login, cadastro, checkout, painel cliente nas 6 abas, master nas 3 abas) × 5 larguras (1440/1024/768/414/360), com Playwright apontando o elemento culpado. Resultado: públicas e painel do cliente 100% OK; único bug eram as **tabelas do master na faixa de tablet (641–1024px)** — muitas colunas, viravam card só em ≤640px e estouravam a largura. Fix: wrapper com `overflow-x: auto` em `#am-lista`/`#am-ultimos` (rolam dentro da caixa; mobile segue em cards; desktop largo cabe). Re-auditoria: **0 telas com overflow**

- [x] Estrutura base do bot (whatsapp-web.js + Express)
- [x] Máquina de estados do atendimento (fluxo.js) — cardápio → opcionais → finalização
- [x] Painel web administrativo (login, cardápio, configurações, conexão, pedidos)
- [x] store.js com recarga ao vivo (mudanças no painel valem sem reiniciar o bot)
- [x] Documentação de deploy (README.md + DEPLOY.md com PM2, VPS e Fly.io)
- [x] Estabilidade do servidor: erros do Puppeteer/WhatsApp não derrubam o painel
- [x] Fluxo por categorias antes de listar itens (reduz tamanho da lista)
- [x] Simulador de conversa no terminal (`testar-bot.js`) e no painel (aba Simulador)
- [x] Taxa de entrega configurável no painel; exibida ao cliente no checkout
- [x] Bot silencioso no estado ATENDENTE (não interfere na conversa do humano)
- [x] Pergunta de bebida suprimida quando cliente já adicionou bebidas ao carrinho
- [x] Deploy no Fly.io com Docker + Chromium do sistema + volume único para dados e sessão
- [x] Migração de pedidos de JSON para SQLite (`better-sqlite3`) — migração automática do legado
- [x] **Horário de funcionamento por dia da semana** — bot responde "fechado" fora do horário; painel com tabela de 7 dias
- [x] **Arquitetura multi-tenant SaaS** — cada empresa tem diretório isolado, banco de pedidos próprio e sessão WhatsApp separada
- [x] Banco mestre de tenants em SQLite (`data/empresas.db`) com autenticação por e-mail + senha
- [x] `src/multi-bot.js` — gerencia um WhatsApp Client por tenant (substitui `bot.js` single-tenant)
- [x] Página de onboarding (`/cadastro.html`) com cadastro e login automático
- [x] Login atualizado para e-mail + senha; cabeçalho do painel exibe nome do restaurante
- [x] Migração automática de instalação legada (cria tenant a partir de `data/config.json`)
- [x] Documentação completa atualizada (CLAUDE.md, README.md, DEPLOY.md, PRD.md)
- [x] **Design system Nymbus Pedidos** — tokens de cor (`--accent`, `--secondary`, `--accent-fg`...), tipografia, componentes; protótipos em `design/prototipos/`; referência visual em `design/UI.md`
- [x] **Redesign Cardápio — editor modal** (Passos 1–4) — modal de edição de item com upload de foto (MIME-validado, path confinado por tenant), builders visuais de composição e opcionais serializando para o formato de texto atual; rota `POST /api/imagem` e `GET /imagens/:slug/:filename` em `src/servidor.js`
- [x] **Redesign Cardápio — lista em cards** (Passo 5) — `renderCardapio()` reescrito: grid 2 colunas desktop / 1 coluna mobile, foto do item, toggle de disponibilidade, botões editar/excluir com ícones SVG; CSS `.cards-grid`/`.item-card` em `style.css`
- [x] **Redesign Login/Cadastro** — layout split (painel de marca gradiente roxo→ciano + área de formulário); logo garfo-e-faca SVG; eye toggle para senha (e confirmação no cadastro); campo `#senha2` e validação de senhas mantidos; `#senha2` omitido do redesign foi corrigido antes da implementação
- [x] **Avisar cliente "pedido pronto"** — `POST /api/pedido/avisar`: envio manual (1 por clique, nunca em massa) de mensagem ao cliente pelo WhatsApp do tenant; templates editáveis em `config.json` (`mensagens.pedidoPronto.entrega`/`.retirada`, variáveis `{cliente}`/`{numero}`); coluna `avisadoEm` no pedido; campos editáveis na aba Configurações
- [x] **Fix `/api/status` sem token** — `atualizarStatus()` no painel chamava `fetch` cru (401) → QR nunca aparecia no front; passou a usar o helper `api()` com `Authorization`. Bug pré-existente que só aflorou quando o QR voltou a gerar (Baileys)
- [x] **Redesign do shell do painel** — sidebar (desktop) + bottom-nav (mobile); home passou de Conexão → Pedidos; um único handler de logout reaproveitado
- [x] **Redesign Pedidos** — métricas com comparativo real vs período anterior (em azul/`--secondary`), filtros com busca, detalhe do pedido em 2 colunas, paginação 10/página ("Mostrando X–Y de N" + `‹ 1 2 … ›`) e datas relativas ("Hoje/Ontem, HH:MM"); pills de tipo mantidas semânticas (Entrega azul / Retirada verde) por decisão de design
- [x] **Redesign Cardápio** — cabeçalho "Gestão de Itens" + botões Nova categoria/Adicionar item, faixa de 3 métricas (total/categorias/indisponíveis), cabeçalho de categoria com ícone + divisória, cards com descrição truncada e rótulo Disponível/Indisponível, card "+" tracejado por categoria
- [x] **Redesign Conexão** — layout 2 colunas (passos "Como conectar" + Dica e painel de QR com moldura gradiente roxo→ciano); 4 estados (desligado/iniciando/aguardando QR/conectado); estado conectado mostra o número do WhatsApp (`getEstado` passou a expor `numero`, capturado de `sock.user.id` no `connection:open`)
- [x] **Redesign Configurações** — card de status do atendimento em destaque, seções com ícone, mensagens em grid (todas preservadas), horários em tabela (desktop) / cards por dia (mobile), formas de pagamento como pills com "+ Adicionar Método", barra inferior com Descartar (recarrega do servidor) + Salvar
- [x] **Redesign Simulador** — "Console de Testes" com chat fiel (avatar + "Nymbus Bot" + horário), painel "Variáveis de Contexto" real (etapa, itens, total do carrinho); mocks sem backend (delay/logs/status da resposta) deliberadamente não construídos
- [x] **Fix bottom-nav mobile** — `.sidebar` no mobile tinha `top:0` herdado + `bottom:0` → barra esticava pra tela toda e cobria o conteúdo; corrigido com `top:auto`
- [x] **Relatórios de pedidos por período** (entregue no redesign de Pedidos) — seletor de período (Hoje / 7 dias / Personalizado) e métricas reais: total de pedidos, média diária, ticket médio e comparativo vs período anterior. Atende ao item de relatórios do ROADMAP (faltaria só export/CSV como item futuro menor)
- [x] **Super-admin — Passo 1/3: backend + autenticação master** (sem tela) — conta master fixa via env (`SUPERADMIN_EMAIL`/`SUPERADMIN_SENHA_HASH`, hash com a mesma `hashSenha` do projeto via `npm run gerar-hash-admin`; sem env → rotas `/api/admin/*` desativadas com 503, sem fallback). Auth isolada (`tokensAdmin` ≠ `tokens`, `exigeSuperAdmin`, `timingSafeEqual`). Rotas: listar/criar tenant, suspender/reativar, excluir. Suspensão com efeito real (login recusado + bot desconectado + tokens do tenant invalidados). Exclusão destrutiva na ordem segura (desconectar → `pedidos.fecharConexao` → `empresas.excluir` apaga linha + pasta) com trava `{ confirmacao: "<slug>" }`. `dotenv` + `.env.example`. Validado por smoke test (15 cenários via curl)
- [x] **Super-admin — Passo 2/3: tela `/admin-master`** — página separada (`public/admin-master.html` + `public/app-admin.js`), sem mistura com o painel de restaurante. Token master em `sessionStorage["tokenAdmin"]` (chave própria; expira ao fechar a aba por escolha de segurança). Login master fiel ao do restaurante (marca gradiente + logo), erros claros (401/503). Listagem em tabela (nome/email/slug/status/criado em) com pills Ativo (verde) / Suspenso (vermelho), estado vazio amigável e responsivo (tabela→cards no mobile). Ações: suspender (modal explicita o impacto: perde acesso + bot desconecta) / reativar / criar (modal nome+email+senha) / excluir (**confirmação forte estilo GitHub** — exige digitar o slug exato para habilitar o botão, envia `{ confirmacao }`). Logout limpa o token master. Validado por Playwright (13 cenários, golden path + mobile + checagem de isolamento dos tokens)
- [x] **Super-admin — Passo 3/3: métricas de uso** — rota `GET /api/admin/metrics` (sob `exigeSuperAdmin`): faixa de 4 cards no topo (total de restaurantes, ativos/suspensos, pedidos no mês somando todos, conectados agora no WhatsApp) + coluna "Pedidos no mês" por tenant na tabela e bolinha de conectado. Métricas **reais**: contagem on-demand no `pedidos.db` de cada tenant (`pedidos.contarNoMes`, reusa o pool, pula `.db` inexistente; nota de cache se crescer p/ centenas de tenants); "conectados" via `multiBot.getEstado`. **Corte do mês no fuso BR** (America/Sao_Paulo, UTC-3) convertido p/ UTC ISO — pedido na virada do mês cai no mês certo. Cards/coluna recarregam após criar/suspender/reativar/excluir. Validado por Playwright + seed de pedidos provando o filtro de fuso na borda do mês
- [x] **Backup manual de dados** — `npm run backup` (`scripts/backup.js`) gera um `backups/backup-AAAA-MM-DD-HHmm.tar.gz` com toda a `data/`. SQLite do app via Online Backup API (`db.backup`, consistente sem downtime); demais `.db` (caches Chromium em pastas órfãs) copiados crus. `backups/` no `.gitignore`; dep `tar` (JS puro). Runbook completo no `DEPLOY.md` (gerar / baixar do Fly / **testar restauração** / restaurar com servidor parado), alerta de FS efêmero no Fly e decisão snapshot+export (S3 fora por ora). Validado: backup gerado + restauração não-destrutiva provando que empresas (3) e pedidos (15) restaurados batem com o original
- [x] **Backup pelo painel super-admin** — aba **Configurações** no `/admin-master` (estrutura pronta p/ novas subseções) com subseção **Backup**: gerar (estado "Gerando…"), listar (arquivo/tamanho/data) e **baixar** pro PC (blob autenticado). Seção "Como restaurar" (somente leitura) com o passo a passo; deixa explícito que a restauração é manual e que o painel não executa. Rotas sob `exigeSuperAdmin` reusam `scripts/backup.js` (refatorado p/ expor `gerarBackup`/`listarBackups`; `npm run backup` intacto); download com **validação anti-path-traversal**. Validado por Playwright + curl (anti-traversal: 4 nomes maliciosos → 400, inexistente → 404, tudo 401 sem token)
- [x] **Fix tela de Backup** — (1) "Como restaurar" passou a ser **texto fixo** no painel (antes lia o `DEPLOY.md` em runtime + parseava markdown → falhava em produção, caía no fallback "não foi possível carregar"); removida a rota `restauracao-doc` e o renderizador de markdown — zero ponto de falha. (2) Listagem **blindada** contra resposta não-JSON: checa `content-type`/status e mostra aviso claro ("reinicie o servidor") em vez de quebrar com `Unexpected token '<'` (sintoma de servidor rodando código antigo, sem a rota). Estado vazio amigável quando não há backup. Validado no fluxo real (Playwright com `backups/` inexistente): lista vazia sem erro, gerar→aparece, runbook sempre visível
- [x] **Fix sessão não limpa (cancelar/sair/pós-pedido)** — no WhatsApp a sessão é guardada sob `${slug}:${jid}`, mas `fluxo.js` chamava `resetSessao(chatId)` com o `jid` cru → apagava chave inexistente e a sessão antiga sobrevivia (cancelar/sair não zeravam; pós-confirmação vazava carrinho e permitia pedido duplicado). Simulador não reproduzia (chave coincide lá). Fix: `limparSessao(sessao)` reseta o **objeto** in-place (independe da chave); `chatId` do canal preservado (avisar intacto). Validado por teste node no caminho real (`slug:jid`): cancelar, sair, pós-confirmação sem duplicar e `chatId` gravado certo
- [x] **HTTPS em produção (Fly.io)** — verificado: `bot-restaurante.fly.dev` serve HTTPS com certificado válido gerenciado pela plataforma e `force_https = true` no `fly.toml` (http→https). Não havia nada a implementar — saiu de "Próximos Passos"; docs (DEPLOY/README/ROADMAP/PRD/CLAUDE) atualizados para refletir que é automático no Fly, com a ressalva de HTTPS manual só em VPS/local
- [x] **Exibição de preço com opcional (bot)** — só texto, cálculo inalterado. No resumo/confirmação, item com opcionais mostra preço base + opcionais + `subtotal` (itálico); sem opcional fica em 1 linha. Helper único `linhasItemPedido()` na revisão e na confirmação. Validado: pedido misto (qtd>1 com opcional + item sem opcional) — total final idêntico centavo a centavo (50,00 esperado = salvo) e subtotal qtd>1 = (base+opcionais)*qtd
- [x] **Pergunta de bebida e observação configuráveis (painel)** — 2 toggles em Configurações → "Comportamento do bot" (`config.atendimento.perguntarBebida`/`perguntarObservacao`, salvos pelo `PUT /api/config`). Default ligado (retrocompatível, lido como `!== false`). `fluxo.js` respeita: bebida OFF pula `PERGUNTA_BEBIDA` (flag é condição a mais sobre a regra atual), observação OFF pula a etapa por item → quantidade. Validado: 5 casos node (incl. bebida OFF **com** categoria de bebida presente, e legado sem campos) + painel real (toggle→salvar→reload persiste→config em disco) + simulador ao vivo (OFF pula as duas; ON volta a perguntar)
- [x] **Saudação com carrinho aberto (bot)** — saudação com carrinho não-vazio pergunta continuar/recomeçar (novo estado `CONFIRMA_REINICIO`) em vez de voltar ao menu mantendo o pedido antigo. "1" mantém o carrinho; "2" zera (`limparSessao`); resposta inválida re-pergunta. Carrinho vazio → menu direto (intacto); `cancelar`/`sair` zeram direto; detecção por match exato (substring como "Rua Bom Dia" não dispara). Validado: 7 casos node + simulador ao vivo
- [x] **Fix GRAVE de isolamento — novo tenant nascia com dados de outro** — cadastro herdava telefone/endereço/horário e cardápio do "Sabor D'Casa" porque `inicializarDiretorio` usava `data/config.json`/`cardapio.json` da raiz (com dados reais, commitados) como template e a cópia preservava os campos. Fix: `configInicial()` limpo inline (identidade vazia, só nome; cardápio vazio) + templates da raiz descontaminados (placeholders). Sem vazamento em runtime (store sempre grava no tenant). Validado: 2 cadastros novos nascem limpos, Sabor intacto. Tenants de teste afetados serão removidos manualmente
- [x] **Limpeza de legado** — removida a migração single-tenant (`migrarLegado`) + arquivos-semente da raiz (`data/config.json`/`cardapio.json`/`pedidos.db`); `.gitignore` enxuto (fora `.wwebjs_*`, `pedidos.json`, `data/*.migrado`, `squads/*`); textos legados corrigidos (`.wwebjs_auth` no painel, comentário de Puppeteer no `index.js`); pastas vazias `.agents`/`.claude` removidas; docs atualizados (1º acesso via `/cadastro.html`, árvore de `data/`). App segue 100% funcional (cadastro+login+tenant limpo validados); todas as deps em uso. Ver `CHANGELOG.md` v0.13.0
- [x] **Assistente de onboarding (1º acesso)** — barra-guia no topo do painel (Dados → Horário → Entrega) que ativa a aba Configurações e rola até a seção já existente (com destaque), sem criar tela nova. Controle por `config.onboardingConcluido`: tenant novo nasce `false` (mostra), antigo `undefined` (não mostra) — o servidor manda, `localStorage` só guarda o passo. Salvar config ou "Pular este passo" avança; "Dispensar"/concluir os 3 → `POST /api/onboarding/concluir` grava `true` e a barra some pra sempre (persiste após relogar). Validado: 12 checks Playwright + visual. Ver `CHANGELOG.md` v0.14.0. **⚠️ REVERTIDO em v0.14.1** (vira wizard no cadastro)
- [x] **Reversão da barra de onboarding no painel** — removida a barra do painel (`#onbBarra`, módulo `app.js`, estilos `.onb-*`, `id` `cfg-sec-*`) por decisão de produto: o onboarding vira wizard no cadastro. Limpeza de `localStorage` `onbPasso:*`. Rota `POST /api/onboarding/concluir` e flag `config.onboardingConcluido` mantidos para avaliação no wizard. Painel de volta ao estado anterior; 8/8 checks Playwright, zero erro de console. Ver `CHANGELOG.md` v0.14.1
- [x] **Wizard de cadastro (4 etapas)** — `cadastro.html` virou wizard Conta → Dados → Horário → Entrega → painel, com barra de progresso "Etapa X de 4" e identidade Nymbus preservada (painel gradiente, logo). Etapa 1 reusa `POST /api/cadastro`+`/api/login` (e-mail duplicado barra na etapa 1, conta criada já loga); etapas 2–4 salvam por `PUT /api/config` (mesma rota do painel, persistência incremental — GET config após login, muta e PUT por etapa). Dados (telefone+endereço) obrigatório; horário e entrega puláveis; "Voltar" simples preserva valores. Abandono: conta existe desde a etapa 1, relogin cai direto no painel (sem retomar wizard). Render das etapas 3/4 inline reusando classes do painel (`.tabela-horarios`, `.pag-*`) — sem duplicar a lógica de persistência. **Código morto removido:** `config.onboardingConcluido` (`empresas.js`) + rota `POST /api/onboarding/concluir` (`servidor.js`), confirmado por grep que nada mais usava. Validado por Playwright no fluxo real: completo (config gravada certa: telefone/endereço/domingo fechado/taxa 7,50/pagamento extra), pular horário+entrega, e-mail duplicado, abandono+relogin, isolamento, zero erro de console no fluxo feliz. Ver `CHANGELOG.md` v0.15.0
- [x] **Texto de horário gerado da tabela** — botão "Gerar automaticamente" sob o campo *Horário (texto exibido ao cliente)* (Configurações) monta o texto pt-BR a partir da tabela de horários ao vivo (agrupa dias seguidos, pula fechados; ex.: "Nosso atendimento é de *Segunda* a *Sexta* das *11:00* às *22:00*"). Não-destrutivo, editável à mão; só front (`resumirHorarios()` em `app.js`), persiste pela rota de config. Validado: 5 casos da lógica + painel real. Ver `CHANGELOG.md` v0.15.1 — 2026-06-11
- [x] **Migração SQLite → Supabase (Postgres + Auth)** — `better-sqlite3` (bancos em disco) → **Postgres gerenciado no Supabase**. Tabelas `empresas` (perfil + `config`/`cardapio` jsonb) e `pedidos` (uma só, isolada por `empresa_id`); schema versionado em `supabase/migrations/` (Supabase CLI). **Login via Supabase Auth** (bcrypt + JWT) — resolve as dívidas P1 de hash forte e sessão persistente de uma vez; super-admin segue env-based. Novos módulos `db.js` (pool pg) e `supabase.js` (clients). Camada de dados ficou async; `store.js` ganhou cache em memória para o `fluxo.js` seguir síncrono. Disco agora só guarda sessões WhatsApp + imagens; `better-sqlite3` removido. Validado ponta-a-ponta: 20 checks de dados/auth + 14 HTTP + pedido completo gravado + wizard→painel→relogin (Playwright). Ver `CHANGELOG.md` v0.16.0 — 2026-06-12
- [x] **App stateless + JWT local + Docker corrigido** — sessões do WhatsApp no Postgres (`wa_auth` + adapter `src/wa-auth.js`, BufferJSON, no lugar do `useMultiFileAuthState`); imagens no Supabase Storage (bucket `cardapio`, `npm run setup-storage`); `exigeAuth` valida o JWT localmente via JWKS (`jose`, sem ida à rede por request — warm ~144ms); `Dockerfile`/`docker-entrypoint.sh` corrigidos (semeavam config.json inexistente com `set -e` → crash no boot; build-deps nativos removidos). App não grava nada em disco → dispensa volume, habilita múltiplas instâncias. `excluir` limpa banco+sessão+Storage. Validado: adapter round-trip (8 checks), bot chega ao QR, upload→Storage→URL pública, JWT warm, exclusão limpa tudo. **Reconexão confirmada em produção** (desconectar→reconectar sem novo QR). Ver `CHANGELOG.md` v0.17.0 — 2026-06-12
- [x] **Higiene de sessões antigas (v0.17.1)** — `limparSessoesAntigas` (em `wa-auth.js`) apaga `session:*` inativas há +90 dias (nova coluna `wa_auth.atualizado_em`), agendada no `index.js` (boot + 24h). Seguro (Baileys recria no próximo contato); não toca creds/pre-keys/app-state. Validado: recente preservada, antiga (100d) removida, creds/pre-key intocados. Ver `CHANGELOG.md` v0.17.1 — 2026-06-12
- [x] **Remoção do backup manual (v0.18.0)** — backup do lado do app virou código morto com o app stateless (a pasta `data/` ficou vazia; o `.tar.gz` empacotava nada). Removidos `scripts/backup.js`, as 3 rotas `/api/admin/backup/*`, a aba **Configurações** inteira do `/admin-master` (HTML+JS+CSS — era exclusiva do backup), o `npm run backup` e a dependência `tar`. Gestão de tenants + métricas intactas; `/admin-master` abre direto em Restaurantes sem barra de abas. Backup agora 100% Supabase (point-in-time recovery). Sintaxe validada (`node -c`) + `npm install` (6 pacotes removidos). Ver `CHANGELOG.md` v0.18.0 — 2026-06-12
- [x] **Aba "Configurações Master" (dados da plataforma Nymbus)** — `plataforma_config` (singleton) estendida com razão social, nome fantasia, CNPJ, endereço, telefone, Facebook, Instagram + credenciais do master (`master_email`/`master_senha_hash`). UI com 3 cards (Dados da empresa · Contato e redes · Dados de acesso). **Credenciais do master migraram pro banco** (editáveis): login lê DB→env bootstrap (`credenciaisMaster()`); `PATCH /api/admin/conta` exige senha atual. Footer da landing passou a exibir esses dados via `GET /api/plataforma/publico`. `src/plataforma.js`. Validado (banco real + Playwright)
- [x] **Footer institucional vertical (estilo Dionísio → colunas)** — de horizontal para multi-coluna (marca + Plataforma · Conta · Legal · Contato), pronto pra crescer; colunas dinâmicas (redes/endereço/telefone) somem se vazias. Centralizado em `public/footer.js` (1 fetch, gancho `window.onPlataformaData` reaproveitado pelas páginas legais). Validado (Playwright desktop/mobile, com e sem dados)
- [x] **Página de Termos de Uso (`public/termos.html`)** — adaptada do exemplo da Dionísio à realidade Nymbus (atendimento no WhatsApp, cardápio, painel; Stripe R$79/7d; **risco de banimento do WhatsApp** em destaque; removidos serviços/IA generativa que a Nymbus não tem). Identidade da empresa injetada de `/api/plataforma/publico`. Fix: o bloco do título era `<header>` e herdava `position:sticky/z-90` do header global (subia por cima do nav ao rolar) → virou `<div>` com gradiente próprio. Validado (Playwright)
- [x] **LGPD Onda 1 — Política de Privacidade + transparência** — `public/privacidade.html` (documento próprio: dados coletados/finalidade/base legal, sub-operadores Supabase/Stripe, retenção honesta, direitos do titular art. 18, DPO dinâmico, declaração de "sem cookies de rastreamento"). **Checkbox de aceite** dos Termos + Privacidade na Etapa 1 do cadastro (trava a criação da conta). Pesquisa prévia de como SaaS BR tratam LGPD (controlador/operador, política obrigatória). Validado (Playwright + caso sem dados)
- [x] **LGPD Onda 2 — autoatendimento de dados + retenção** — `GET /api/conta/exportar` (baixa JSON com empresa+config+cardápio+todos os pedidos: acesso+portabilidade); `DELETE /api/conta` (excluir a própria conta — exige senha atual + digitar "EXCLUIR"; reusa `empresas.excluir`); `pedidos.anonimizarAntigos(12)` agendado no `index.js` (boot+24h) anonimiza PII de pedidos >12 meses mantendo número/itens/total/datas. UI "Privacidade e dados" + zona de perigo na sub-aba Empresa. Validado E2E (cadastro→export→travas→exclusão→login 401; SQL read-only; Playwright)
- [x] **Termos/Privacidade em modal no cadastro** — clicar no aceite abre os documentos num **modal (iframe em modo `?embed`)** sobre o cadastro, em vez de navegar pra outra página (evita perda de contexto/conversão). Fonte única de verdade (carrega a própria página sem nav/footer via classe `.embed`); identidade dinâmica preservada; acessível (foco, Esc, overlay, tela cheia no mobile); "Li e aceito" marca o checkbox; links entre documentos mantêm o embed. Validado (Playwright desktop/mobile + navegação cruzada)
- [x] **Billing reflete no Stripe ao suspender/excluir** — antes, excluir/suspender não tocavam no Stripe → **assinatura órfã cobrando o cartão**. Agora: **excluir** (self `DELETE /api/conta` + master `DELETE tenant`) **cancela** a assinatura ANTES de apagar; se falhar, **aborta (502)** e orienta o suporte. **Suspender** (master) **pausa** a cobrança (`pause_collection:void`, reversível); **Reativar** **retoma**; falha no Stripe → bloqueio acontece e devolve `avisoStripe` (toast). `stripe.cancelarAssinatura` idempotente + novos `pausarAssinatura`/`retomarAssinatura`. Alerta de "assinatura ativa será cancelada" ao abrir Excluir conta. **Validado E2E contra o Stripe de teste real** (helpers: pausar→void/retomar→null/cancelar→canceled/idempotência; self-delete cancela e login 401; **master suspender→paused / reativar→retomada / excluir→canceled** via painel real)
- [x] **Segurança — Auditoria + Onda 1 (blindagem de borda)** — auditoria das 11 frentes do `docs/auditoria-seguranca.md` (5 auditores em paralelo). Veredito: **isolamento multi-tenant sólido (sem IDOR)**, webhook Stripe verifica assinatura, sem dado de cartão no banco. **Onda 1 implementada:** (1) **helmet + CSP estrita** (`script-src` sem `unsafe-inline`, frame só Stripe, anti-clickjacking, HSTS, nosniff) — pré-requisito foi **extrair todo JS inline** do front p/ arquivos `.js` (9 blocos + 9 handlers) → novos `login/checkout/cadastro/landing/legal-embed/termos/privacidade.js`; (2) **rate limit** (`express-rate-limit`) + `trust proxy`: login master 5/15min, restaurante 10/15min, cadastro 5/h, assinatura 20/15min; (3) **`form-data` vulnerável corrigido** (`npm audit fix`, audit zerado). Validado no navegador (Playwright: 0 violações de CSP no console, toggles de senha, modal de Termos e iframe `?embed` OK). Ver `CHANGELOG.md` v0.22.0 — 2026-06-16. **Ondas 2 e 3 pendentes** (ver "📋 Próximos Passos")
- [x] **Segurança — Onda 2 (código: M1, M4, M5, M6)** — **M4** `pedidos.anonimizarAntigos` passou a limpar a `observacao` dentro do jsonb `itens` (PII de texto livre que escapava da retenção; idempotente, guard de array). **M5** 3 rotas (`confirmar assinatura`, `bot/resetar`, `pedido/avisar`) pararam de devolver `e.message` cru no erro 500 — texto fixo + detalhe só no log. **M1** `PUT /api/config` e `/api/cardapio` validam payload (objeto, cap de tamanho 256/512 KB, ≤200 categorias, ≤500 itens/cat) sem schema rígido. **M6** exclusão de conta loga as falhas best-effort de Auth/Storage (em vez de silenciar) p/ reconciliação. Validado: M1 (9 casos da lógica) + M4 (transform jsonb no banco real, idempotência) + carga do servidor. Ver `CHANGELOG.md` v0.22.1 — 2026-06-16
- [x] **Segurança — Onda 2 (A3: histórico do git purgado)** — `git filter-repo --invert-paths` removeu `data/config.json` e `data/cardapio.json` de **todo o histórico** (93 commits reescritos) — eliminando a senha morta `admin123` e a PII de teste do "Sabor D'Casa". Backup completo antes (`../bot-restaurante-backup-pre-filterrepo.bundle`). Verificado: arquivos ausentes em `git log --all`, código intacto, novos SHAs. **`git push --force origin main`** feito (`fb027b8`→`f5775bb`). Repo é público → purga justificada. **Ressalva:** GitHub pode reter SHAs antigos em cache até GC (purga total = ticket ao Support). Onda 2 da auditoria **fechada**
- [x] **Segurança — Onda 3 (hardening do super-admin + refinos)** — **M3** hash master SHA-256 → **bcrypt** (`bcryptjs`, JS puro) com `verificarSenhaMaster` que **detecta o formato** (migração graciosa: hash legado segue válido até trocar a senha); `gerar-hash.js` emite bcrypt. **Token master**: `crypto.randomBytes` (CSPRNG) + **TTL de 12h** no Map. **Upload**: valida **magic bytes** (assinatura real vira fonte de verdade; MIME do header descartado). **Refinos**: `escapar()` do `app.js` cobre aspa simples; `unhandledRejection` loga só `.message`; `.gitignore` cobre `.env.*`/`baileys-*/`. **M2 + RLS documentados como risco aceito** (não exigem código). Validado: bcrypt novo+legado verificam, senha errada falha, magic bytes (JPEG/PNG/WebP + rejeita falso), escapar — todos OK; servidor carrega; `gerar-hash` emite `$2b$`. Ver `CHANGELOG.md` v0.22.2 — 2026-06-16. **Auditoria de segurança (Ondas 1–3) concluída**
- [x] **Suíte de testes automatizada + CI** — `npm test` com o runner nativo **`node:test`** (zero dependência): cobre a lógica pura crítica em `test/` (validação de payload config/cardápio, magic bytes, hash master bcrypt + migração SHA-256 legado, geração de slug) — **16 testes, usam env dummy → rodam sem segredos**. `npm run check` (`scripts/check-syntax.js`) varre a sintaxe de `src/`+`scripts/`+`index.js`. **CI** em `.github/workflows/test.yml` roda `check` + `test` a cada push/PR. Refactor de apoio: validadores + magic bytes extraídos do `servidor.js` para `src/validacao.js` (puro/testável, sem mudança de comportamento). Validado: 16/16 verde, check OK (17 arquivos), servidor carrega. Ver `CHANGELOG.md` v0.22.3 — 2026-06-16
- [x] **Segurança — M2 (anti-enumeração) + RLS hardening** — **M2:** `POST /api/cadastro` passou a responder uma **mensagem genérica e uniforme** em qualquer falha ("Não foi possível concluir o cadastro… se já tiver conta, faça login"), sem confirmar se o e-mail existe; o detalhe vai só pro log. A rota master (`/api/admin/tenants`, sob `exigeSuperAdmin`) mantém o "já cadastrado" (sem risco de enumeração). **RLS:** migration `20260616120000_rls_hardening.sql` — `enable row level security` reafirmado + `revoke all ... from anon, authenticated` + `comment` nas 4 tabelas. Escolha consciente de **não adicionar policies** (abririam o caminho PostgREST hoje fechado); o hardening vai na direção de *mais* trancado. Aplicado via `supabase db push`. Verificado: RLS on + **0 grants** anon/authenticated em todas; servidor carrega; testes 16/16. Ver `CHANGELOG.md` v0.22.5 — 2026-06-16
- [x] **Export CSV de pedidos** — botão **Exportar** no header da aba Pedidos gera um CSV (Excel BR: separador `;` + BOM) com os pedidos **atualmente filtrados** (período + tipo + busca): número, data, cliente, telefone, tipo, endereço, pagamento, itens (resumo com observação), total, avisado. Client-side (espelha o export de clientes do master); arquivo `pedidos-AAAA-MM-DD.csv`. Header com `flex-wrap` p/ não estourar no mobile. Fecha o "resta só o export" do ROADMAP P3. Validado (sintaxe + lógica do CSV; UI não validada visualmente — exige sessão logada)
- [x] **Correção do CI de deploy + documentação** — removido o workflow de deploy automático do Fly (`fly-deploy.yml`), que falhava a cada push por falta do segredo `FLY_API_TOKEN` (o deploy é manual via `fly deploy`) → pararam os e-mails de falha; `test.yml` (CI de testes) atualizado para `checkout@v5`/`setup-node@v5`; `DEPLOY.md` ganhou a seção "Deploy automático (CI) — opcional" com o passo a passo pra ligar quando quiser. — 2026-06-16
- [x] **README modernizado** — atualizado ao estado atual: Node 22 (não 20), modelo pago/Stripe + aba Assinatura, `.env` completo (super-admin + Stripe + suporte), seção de testes (`npm test`/`check` + CI), marca Nymbus Pedidos, abas do painel e árvore de arquivos; `.env.example` corrigido (hash master SHA-256 → bcrypt). — 2026-06-16
- [x] **Domínio próprio `pedidos.nymbuslab.com.br`** — subdomínio criado na Hostinger (CNAME `pedidos` → `bot-restaurante.fly.dev`) + `fly certs add` (Let's Encrypt, `Status = Issued`, HTTPS automático gerenciado pelo Fly). App responde no domínio novo (HTTP 200 + TLS OK); o `.fly.dev` segue funcionando em paralelo. **Sem mudança de código** — `success_url`/`return_url` do Stripe derivam do host (`baseUrlDe` em `src/servidor.js`). Passo a passo registrado em [DEPLOY.md](DEPLOY.md) (seção *Domínio próprio*). Stripe: na auditoria descobriu-se que **não há webhook cadastrado** e o app roda em **Área restrita (teste)** → migração do webhook/chaves vira o **checklist de go-live** (ver Próximos Passos + [docs/assinatura-stripe.md](docs/assinatura-stripe.md)). — 2026-06-16
- [x] **Stripe em produção (go-live)** — saiu da Área restrita (sandbox) e foi pra **produção (live)**, cobrando de verdade. Produto **"Plano Essencial"** (`prod_UjIR38QCFMhn9E` / `price_1Tjpqo...`, **R$ 79,00 · mensal · BRL**, livemode confirmado via API antes de aplicar). **Webhook live criado** (`we_1TjrFC...`, status `enabled`, 6 eventos → `https://pedidos.nymbuslab.com.br/api/stripe/webhook`); o `whsec` foi capturado na criação e aplicado direto no Fly **sem trafegar em texto**. **4 secrets** (`STRIPE_SECRET_KEY`/`PUBLISHABLE_KEY`/`PRICE_ID`/`WEBHOOK_SECRET`) trocados pelas versões live via `fly secrets set` → redeploy OK. Verificado: secrets `Deployed`, webhook responde **400** a POST não assinado (rota viva + verificação de assinatura ativa). **Nome do plano** no front trocado de "Plano Nymbus Pedidos" → **"Plano Essencial"** ([admin.html](public/admin.html)/[checkout.html](public/checkout.html), commit `bed11b7`) — preparando o terreno pro 2º plano. **`fly deploy` feito** → nome novo + as 3 entregas pendentes da sessão anterior no ar. **Pendente (usuário):** teste com cartão real. Doc atualizado em [docs/assinatura-stripe.md](docs/assinatura-stripe.md). — 2026-06-18
