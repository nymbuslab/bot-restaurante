# Lacunas da base — pdv-balcao-em-aberto

- **Rate limits, tamanho máximo de carrinho ou de nº de rodadas por pedido/mesa** — não encontrado em nenhum dos arquivos lidos (`servidor.js`, `pedidos.js`, `mesas.sql`, `mesas-db.js`). Não parece ser uma preocupação de produto hoje (mesas também não têm limite de rodadas) — sinalizado só por completude, não parece exigir decisão do usuário.
